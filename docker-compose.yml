services:
  db:
    image: postgres:latest
    volumes:
      - social-media-db-data:/var/lib/postgresql/data/pgdata
    env_file:
      - .env
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"

volumes:
  social-media-db-data:
    driver: local